#cloud-config
package_update: true
package_upgrade: true
packages:
  - ca-certificates
  - curl
  - docker.io
  - docker-compose-plugin
  - restic
users:
  - default
  - name: deployer
    groups: [sudo, docker]
    shell: /bin/bash
    sudo: "ALL=(ALL) NOPASSWD:ALL"
    ssh_authorized_keys:
%{ for key in ssh_public_keys ~}
      - ${key}
%{ endfor ~}
runcmd:
  - mkdir -p /opt/stack-and-scale/deployments /opt/stack-and-scale/scripts /opt/stack-and-scale/secrets /var/lib/stack-and-scale/monitoring
  - chown -R deployer:deployer /opt/stack-and-scale
  - chown -R deployer:deployer /var/lib/stack-and-scale
  - systemctl enable --now docker
