# Copy outside the repository as .env.database.production.
POSTGRES_USER=
POSTGRES_PASSWORD=
# The database server's private Hetzner address, not its public address.
PRIVATE_DATABASE_BIND_ADDRESS=10.48.1.20
