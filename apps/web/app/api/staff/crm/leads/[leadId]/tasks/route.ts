import { NextResponse } from "next/server";
const apiOrigin = process.env["API_PUBLIC_URL"] ?? "http://127.0.0.1:3100";
export async function POST(request: Request, context: Readonly<{ params: Promise<{ leadId: string }> }>) { const { leadId } = await context.params; try { const response = await fetch(`${apiOrigin}/api/v1/crm/leads/${encodeURIComponent(leadId)}/tasks`, { method: "POST", headers: { cookie: request.headers.get("cookie") ?? "", "content-type": "application/json", "x-correlation-id": request.headers.get("x-correlation-id") ?? crypto.randomUUID() }, body: await request.text(), signal: AbortSignal.timeout(8_000) }); return new NextResponse(await response.text(), { status: response.status, headers: { "content-type": "application/json", "cache-control": "no-store" } }); } catch { return NextResponse.json({ error: "CRM is temporarily unavailable." }, { status: 503 }); } }
