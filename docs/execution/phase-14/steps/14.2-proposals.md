# Step 14.2 — Proposal Workflow

Build draft proposal versions, line items, optional items, validity dates,
approval, publication, viewing and acceptance using the shared primitives.

Acceptance is recorded as evidence bound to one immutable version. It is not
described as a qualified digital signature until the e-sign provider gate passes.
